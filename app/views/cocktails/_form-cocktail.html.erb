<%= simple_form_for(@cocktail) do |f| %>
  <div class="form-inputs">
    <%= f.input :name, required: true %>
    <%= f.input :img_url, required: true, input_html: { rows: 5 } %>
  </div>
  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
